module.exports = {
  /*
  This will be default values for any field named `tags` or ending in `_tags` (multiselect) or `tag` or ending in `_tag` (select)
  */
  tags: [
    "Nature",
    "Fiction",
    "Nonfiction",
    "Poetry",
    "Parenthood",
    "Politics",
    "Publishing",
    "Reading",
    "Relationships",
  ],
}